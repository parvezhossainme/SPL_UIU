 // i=strlen(str1);
    // for(k=0; str2[k] !='\0'; k++)
    // str1[i+k] = str2[k];
    // str1[i + k] = '\0';
    // puts(str2);
    // printf("\n");
    // puts(str1);
    // printf("\n");
    // strrev(str1);
    // puts(str1);